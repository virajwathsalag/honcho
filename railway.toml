[build]
builder = "docker"
buildImage = "python:3.13-slim-bookworm"

[deploy]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "honcho-api"
source = "."

[variables]
ENV = "production"